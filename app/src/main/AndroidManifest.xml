<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.expressdelivery">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpressDelivery"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".orderdetails.Thankyou_screen"></activity>
        <activity android:name=".orderdetails.Total_bill" />
        <activity android:name=".orderdetails.Order_activity"
            android:label="Your cart"/>
        <activity android:name=".dialogbox" />
        <activity android:name=".food_items"
            android:parentActivityName=".Home"/>
        <activity android:name=".Home"
            android:parentActivityName=".LoginScreen"/>
        <activity android:name=".LoginScreen"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SignUpScreen"
            android:parentActivityName=".LoginScreen" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>